using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class player_controller : MonoBehaviour
{
    //UI_placeholder

    //misc variables
    
    // Start is called before the first frame update
    void Start()
    {
        //initiate players starting values tbd
        
    }


    
    // Update is called once per frame
    void Update()
    {

        //Debug.Log(wood);
        //had to cast the UI text to a string in order to manipulate it.

        //food value is always increasing for now.
        //food = increase_resource();
        //Debug.Log("tick:" + tick + "/" + delay + "lumber;" + lumber);

    }
}
